machine:
  php:
    version: 7.0.4

dependencies:
  cache_directories:
    - ~/.composer/cache
  override:
    - ./composer.phar install

test:
  override:
    - ./composer.phar run lint
    - ./composer.phar run test
  post:
    - bash <(curl -s https://codecov.io/bash)
