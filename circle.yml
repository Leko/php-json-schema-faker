machine:
  php:
    version: 7.0.4

dependencies:
  override:
    - ./composer.phar install

test:
  override:
    - ./composer.phar run lint
    - ./composer.phar run test
  post:
    - bash <(curl -s https://codecov.io/bash)
